```bash
# Setup two WSL2 distros to simulate different hosts: Ubuntu and Debian, for example.
# On first host (as a receiver side):
$ make
$ make runr

# On second host (as a sender side):
$ make
$ make runs

```